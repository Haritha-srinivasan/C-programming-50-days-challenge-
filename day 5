#include<stdio.h>
int main(){
       int a[5]={7,2,3,1,5},i=0,c=0;
       c=a[i+1];
       for(i=0;i<5;i++){
        if(a[i]<=c){
            c=a[i];
            }
             else{
                a[i]=a[i+1];
             }
             }
       printf("%d",c);
        
        return 0;
}
